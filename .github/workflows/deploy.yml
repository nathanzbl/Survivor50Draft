name: Deploy
on:
  push:
    branches: [master]
  workflow_dispatch:
jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Deploy
        run: |
          cd /Users/nathanblatter/Desktop/survivor50draft
          git pull origin master
          docker compose down
          docker compose build --no-cache
          docker compose up -d
